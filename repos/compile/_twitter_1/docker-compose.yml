version: "3.8"
services:
  system:
    image: "twitter"
    container_name: emerson-twitter
    build: .
    tty: true
    command: 
      - /bin/sh
      - -c
      - |
        tail -f /etc/hosts
    